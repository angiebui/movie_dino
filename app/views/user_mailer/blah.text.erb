<!DOCTYLE html>
<html>
  <head>
    <style type='text/css'>
      h3 { color: #f00; }
      ul { list-style: none; }
    </style>
    <meta context='text/html; charset=UTF-8' http-equiv="Content-Type"/>
  </head>
  <body>

<!-- HEADER HERE -->

    <h2> It's time to watch a movie! </h2>
    <p> <%= @user.first_name.capitalize %>, Dash found a few great movies for you and your friends. Check it out:</p>

    <ul>
      <% @outing.selections.top_picks.limit(3).each_with_index do |selection, index| %>
        <li>
          <h4>#<%= index + 1 %>.</h4>
        </li>
        <li>
          <%= image_tag(selection.movie.poster_med) %>
        </li>
        <li>
          <%= selection.movie.title.titleize %> at <%= selection.showtime.time_in_timezone %>
       </li>
       <li>
          <%= selection.theater.name %>
       </li>
       <li>
          <%= selection.theater.address %>
       </li>
      <% end %>
    </ul>
    <%= link_to "See your stats", @user %>
  </body>

</html>

<!-- FOOTER HERE -->
